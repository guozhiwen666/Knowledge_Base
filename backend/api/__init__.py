"""接口层（FastAPI 路由）。

严格按文档第 8 章划分，一个分组一个模块，共 **21 个路由**，一个不多一个不少：

================  ==========================================================
模块              路由分组
================  ==========================================================
``auth``          ``/api/auth/login``（8.2，1 个）
``org``           ``/api/org/**``（8.3，5 个）
``knowledge``     ``/api/knowledge/**``（8.4，7 个）
``ai``            ``/api/ai/chat/stream``（8.5，1 个）
``dashboard``     ``/api/dashboard/**``（8.6，4 个）
``settlement``    ``/api/settlement/**``（8.7，3 个）
================  ==========================================================

8.3 与 8.4 里带 ``/`` 的写法（如 ``POST /api/org/users`` / ``PUT /api/org/users/{id}``）
按两个路由分别实现，因此实际代码里的路由数是 21。

需求未列出的接口（用户列表、部门增删改、知识单元手工新建、解析进度、
已发布 FAQ 库、知识缺口一键建档）**一律不实现**，全部登记在文档第 14 章【待确认】。

本 ``__init__`` 不做导入，与其他包保持一致。
"""
