"""核心基础层：配置、数据库、客户端、鉴权依赖、统一响应。

模块划分：

====================  ==========================================================
模块                  内容
====================  ==========================================================
``config``            从 .env 读取全部运行参数（唯一读环境变量处）
``db``                引擎 / 会话工厂 / FastAPI 会话依赖
``cache``             缓存客户端（Redis 优先、进程内兜底）
``storage``           MinIO 与 Milvus 客户端工厂
``llm``               向量化函数与流式生成函数（OpenAI 兼容协议）
``security``          登录态校验与 RBAC 操作权限依赖
``response``          统一响应包装与异常处理（8.1 约定）
``container``         应用级单例组件容器
====================  ==========================================================

与 ``models`` / ``services`` 的处理一致：本 ``__init__`` 不做导入，
避免"只想用某一个模块"的环境被整条依赖链拖住。
"""
