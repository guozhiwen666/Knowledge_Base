"""LangGraph 编排层。

按文档 4.1，本层由 **1 张在线主图 + 2 张离线子图** 组成：

==========================================  =========================================
模块                                        作用
==========================================  =========================================
``state``                                   ``QAState`` 全局状态定义（4.2）
``events``                                  SSE 事件下发（4.8 的事件契约）
``qa_graph``                                在线主图：A1 →(A2 ∥ A3)→ 判定 → A4→A5→A6→A7
``settlement_graph``                        离线子图：知识沉淀挖掘（4.5）
``metrics_graph``                           离线子图：看板聚合并预热缓存（4.6）
==========================================  =========================================

Agent 定义在 ``agents`` 包，本层只负责把它们连成图。

与 ``models`` / ``services`` / ``core`` 的处理一致：本 ``__init__`` 不做导入，
避免"只想用某一张图"的环境被整条依赖链（Milvus / 模型 / 缓存）拖住。
"""
